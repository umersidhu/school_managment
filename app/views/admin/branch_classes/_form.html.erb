<%= form_for [:admin, @branch_class], html:{class:"form-horizontal", "data-parsley-validate" => true } do |f| %>
  <% if @branch_class.errors.any? %>
    <div id="error_explanation">
      <ul>
        <% @branch_class.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div class="form-group">
    <%= f.label "", class:"control-label col-sm-2" do %>
      Class Name:<span class="text-danger">*</span>
    <% end %>
    <div class="col-sm-10">
      <%= f.text_field :name, class: "form-control", placeholder: "Enter branch Name",required: true, "aria-required" => true %>
    </div>
  </div>
  <div class="form-group"> 
    <div class="col-sm-offset-2 col-sm-10">
      <%= f.submit "Submit", class: "btn btn-success" %>
      <%= link_to "Back", admin_branch_classes_path, class: "btn btn-primary" %>
    </div>
  </div>
<% end %>
